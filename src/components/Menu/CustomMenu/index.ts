import CustomMenuButton from "./CustomMenuButton";
import CustomMenuRoot from "./CustomMenuRoot";
import CustomMenuSection from "./CustomMenuSection";

export const Menu = {
  Root: CustomMenuRoot,
  Section: CustomMenuSection,
  Button: CustomMenuButton
}